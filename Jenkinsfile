pipeline {
    agent any

    stages {
        stage('Date and Time') {
            steps {
                // Execute the shell script
                sh './script.sh'
            }
        }
    }
}

